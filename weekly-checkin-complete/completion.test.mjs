import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {completeWeeklyCheckin} from './completion.js';
const token='A'.repeat(43);
test('completion page starts neutral, not successful',()=>{const html=fs.readFileSync(new URL('./index.html',import.meta.url),'utf8');assert.match(html,/Finishing your weekly check-in/);assert.doesNotMatch(html,/>All done!</);});
test('success UI state occurs only after a successful POST',async()=>{const states=[];await completeWeeklyCheckin({token,fetchImpl:async()=>({ok:true,status:200}),onState:value=>states.push(value)});assert.deepEqual(states,['finishing','success']);});
test('invalid or expired completion never enters success state',async()=>{for(const status of [400,404,410]){const states=[];await completeWeeklyCheckin({token,fetchImpl:async()=>({ok:false,status}),onState:value=>states.push(value)});assert.deepEqual(states,['finishing','invalid']);assert.ok(!states.includes('success'));}});
test('failed POST exposes honest retry state',async()=>{const states=[];await completeWeeklyCheckin({token,fetchImpl:async()=>{throw new Error('offline')},onState:value=>states.push(value)});assert.deepEqual(states,['finishing','retry']);const source=fs.readFileSync(new URL('./completion.js',import.meta.url),'utf8');assert.match(source,/We couldn't confirm your weekly check-in/);assert.match(source,/retry\.hidden = state !== 'retry'/);});
